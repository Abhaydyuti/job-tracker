<%- include('../partials/header') %>

<div class="row justify-content-center">
  <div class="col-lg-7">

    <div class="d-flex align-items-center mb-4">
      <a href="/jobs" class="btn btn-outline-secondary btn-sm me-3">
        <i class="bi bi-arrow-left"></i> Back
      </a>
      <h2 class="fw-bold mb-0">Edit Application</h2>
    </div>

    <div class="card shadow-sm border-0">
      <div class="card-body p-4">

        <!-- Errors -->
        <% if (errors.length > 0) { %>
          <div class="alert alert-danger">
            <ul class="mb-0 ps-3">
              <% errors.forEach(e => { %><li><%= e.msg %></li><% }) %>
            </ul>
          </div>
        <% } %>

        <!-- PUT request via method-override -->
        <form action="/jobs/<%= job.id %>?_method=PUT" method="POST" novalidate>

          <div class="row g-3">

            <!-- Company Name -->
            <div class="col-md-6">
              <label class="form-label fw-semibold">Company Name <span class="text-danger">*</span></label>
              <input type="text" name="company_name" class="form-control"
                value="<%= old.company_name || '' %>" required />
            </div>

            <!-- Role -->
            <div class="col-md-6">
              <label class="form-label fw-semibold">Role / Job Title <span class="text-danger">*</span></label>
              <input type="text" name="role" class="form-control"
                value="<%= old.role || '' %>" required />
            </div>

            <!-- Status -->
            <div class="col-md-4">
              <label class="form-label fw-semibold">Status <span class="text-danger">*</span></label>
              <select name="status" class="form-select" required>
                <% statusOptions.forEach(s => { %>
                  <option value="<%= s %>" <%= old.status === s ? 'selected' : '' %>>
                    <%= s %>
                  </option>
                <% }) %>
              </select>
            </div>

            <!-- Applied Date -->
            <div class="col-md-4">
              <label class="form-label fw-semibold">Applied Date</label>
              <input type="date" name="applied_date" class="form-control"
                value="<%= old.applied_date ? old.applied_date.toString().substring(0,10) : '' %>" />
            </div>

            <!-- Interview Date -->
            <div class="col-md-4">
              <label class="form-label fw-semibold">Interview Date</label>
              <input type="date" name="interview_date" class="form-control"
                value="<%= old.interview_date ? old.interview_date.toString().substring(0,10) : '' %>" />
            </div>

            <!-- Job Link -->
            <div class="col-12">
              <label class="form-label fw-semibold">Job Posting Link</label>
              <input type="url" name="link" class="form-control"
                value="<%= old.link || '' %>" />
            </div>

            <!-- Notes -->
            <div class="col-12">
              <label class="form-label fw-semibold">Notes</label>
              <textarea name="notes" class="form-control" rows="3"><%= old.notes || '' %></textarea>
            </div>

          </div>

          <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-success px-4">
              <i class="bi bi-check-circle me-1"></i>Save Changes
            </button>
            <a href="/jobs" class="btn btn-outline-secondary">Cancel</a>
          </div>

        </form>

        <hr class="my-4"/>

        <!-- Delete button — separate form with DELETE override -->
        <form action="/jobs/<%= job.id %>?_method=DELETE" method="POST"
              onsubmit="return confirm('Are you sure you want to delete this application?')">
          <button type="submit" class="btn btn-outline-danger btn-sm">
            <i class="bi bi-trash me-1"></i>Delete This Application
          </button>
        </form>

      </div>
    </div>

  </div>
</div>

<%- include('../partials/footer') %>